# Voice dictation under Hyprland — placeholder until Phase 4.
#
# The sunoto daemon currently registers its own global push-to-talk grab on
# X11 only. Wayland activation (XDG Global Shortcuts portal) and insertion
# (AT-SPI / portals) are Phase 4 work; until then there is nothing to bind
# here. The GTK overlay itself already supports layer-shell and will anchor
# natively once the daemon runs under Hyprland.
#
# Source this from your ~/.config/hypr/bindings.conf once Phase 4 lands:
#   source = ~/.config/hypr/voice-dictation.conf
